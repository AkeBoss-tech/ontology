{
  "name": "ontology-ui-framework",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=census-example",
    "dev:app": "npm run dev --workspace",
    "build:app": "npm run build --workspace",
    "test": "npm run test --workspaces",
    "create-app": "../scripts/create-app.sh"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}



